/// Reusable UI components shared across screens.
///
/// Widgets placed here should be presentation-only — they receive data
/// and callbacks via constructor parameters, not direct provider access,
/// unless they are intentionally scoped feature widgets.
library;

// Shared widgets (canvas, color swatch, toolbar) will be added later.
