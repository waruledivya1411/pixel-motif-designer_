import 'package:flutter/foundation.dart';

/// Manages the active color palette and selected paint color.
///
/// Isolating palette state allows the color picker toolbar to rebuild
/// independently of the canvas during rapid pixel painting.
class PaletteProvider extends ChangeNotifier {
  // Palette selection logic will be implemented in a later phase.
}
